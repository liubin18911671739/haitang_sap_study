# 海棠杯 SaP 研究 - 完整可复现分析项目

**Students as Partners 视角下资源约束高校图书馆生成式AI素养教育的馆社共创机制与效能研究——以"海棠杯"为例**

## 📋 项目概述

本项目实现了海棠杯 SaP 研究的完整实证分析管线，包括：
- 外部基线校准
- 前后测效能评估
- 量表信效度分析
- 馆社共创机制量化
- 结构方程模型（SEM）
- 学习分析（行为日志）
- 质性三角互证

## 📁 项目结构

```
haitang_sap_study/
├── data/                           # 数据目录
│   ├── external/                   # 外部基线数据
│   │   ├── external_ai_literacy.csv
│   │   └── external_ai_readiness.csv
│   └── haitang_local/              # 海棠杯本地数据
│       ├── haitang_pre.csv
│       ├── haitang_post.csv
│       ├── haitang_cocreate.csv
│       ├── haitang_engagement_ose.csv
│       ├── haitang_behavior_log.csv
│       └── haitang_qual_coded.xlsx
├── outputs/                        # 输出目录
│   ├── tables/                     # 统计表格
│   ├── figs/                       # 图表
│   └── models/                     # 模型报告
├── src/                            # 源代码
│   ├── main.py                     # 主程序（一键运行）
│   ├── config.py                   # 配置文件
│   ├── scales.py                   # 量表条目映射
│   ├── stats_utils.py              # 统计工具
│   ├── sem_models.py               # SEM 模型
│   ├── learning_analytics.py       # 学习分析
│   └── qualitative_matrix.py       # 质性分析
├── requirements.txt                # Python 依赖
├── README.md                       # 本文件
└── instruction.md                  # 详细需求文档
```

## 🚀 快速开始

### 1. 环境配置

**Python 版本要求**: 3.10+

```bash
# 创建虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt
```

### 2. 准备数据

将数据文件放置到对应目录：

#### 外部基线数据（`data/external/`）
- `external_ai_literacy.csv`: AI Literacy Questionnaire 外部数据
- `external_ai_readiness.csv`: 跨国 AI readiness 外部数据

#### 海棠杯本地数据（`data/haitang_local/`）
- `haitang_pre.csv`: 前测数据（包含 GenAI 四维条目）
- `haitang_post.csv`: 后测数据（包含 GenAI 四维条目）
- `haitang_cocreate.csv`: 共创过程七维数据
- `haitang_engagement_ose.csv`: OSE 参与度四维数据
- `haitang_behavior_log.csv`: 行为日志数据
- `haitang_qual_coded.xlsx`: NVivo 导出的质性编码数据

**注**: 如果某些数据文件不存在，程序会跳过相应模块并继续执行。

### 3. 运行分析

```bash
cd src
python main.py
```

程序将自动运行所有7个分析模块，结果保存在 `outputs/` 目录。

## 📊 数据格式要求

### 前后测数据（`haitang_pre.csv` / `haitang_post.csv`）

必需列（默认列名，可在 `src/scales.py` 中修改）：

```
user_id, AI基础知识1, AI基础知识2, AI基础知识3,
AI核心技能1, AI核心技能2, AI核心技能3,
AI伦理意识1, AI伦理意识2, AI伦理意识3,
创新思维1, 团队协作1, 问题解决1
```

### 共创过程数据（`haitang_cocreate.csv`）

```
user_id, pos_interdep, indiv_account, collaboration,
shared_mental, safe_env, creative_comm, group_reflect
```

### 参与度数据（`haitang_engagement_ose.csv`）

```
user_id, ose_skill, ose_emotion, ose_particip, ose_perf
```

### 行为日志（`haitang_behavior_log.csv`）

```
user_id, action, ts, duration
```

其中 `action` 可取值：`view`, `discuss`, `cocreate`, `submit`, `revise`

### 质性编码（`haitang_qual_coded.xlsx`）

```
case_id, dim, weight
```

其中 `dim` 应包含：`empowerment`, `belonging`, `learning_gain`, `relation_shift`, `org_improvement`

## 📈 输出结果说明

### 表格输出（`outputs/tables/`）

| 文件名 | 说明 | 对应论文 |
|--------|------|---------|
| `external_ai_literacy_baseline.csv` | 外部 AI Literacy 基线 | 3.1 节 |
| `external_ai_readiness_baseline.csv` | 外部 AI Readiness 基线 | 3.1 节 |
| `pre_post_ai_lit.csv` | 前后测配对 t 检验结果 | 表1 |
| `ai_lit_alpha.csv` | Cronbach's Alpha 信度 | 3.2 节 |
| `efa_report.txt` | 探索性因子分析报告 | 3.2 节 |
| `sem_dataset.csv` | SEM 分析数据集 | - |
| `sem_fit_indices.csv` | SEM 拟合指标 | 表2 |
| `sem_path_coefficients.csv` | SEM 路径系数 | 表3 |
| `behavior_features_clustered.csv` | 用户行为特征+聚类 | 4.1 节 |
| `behavior_auc.csv` | 产出预测 AUC | 表4 |
| `sap_outcome_matrix.csv` | SaP 结果证据矩阵 | 表5 |
| `sap_dimension_summary.csv` | SaP 维度统计摘要 | 5.1 节 |

### 模型报告（`outputs/models/`）

| 文件名 | 说明 |
|--------|------|
| `sem_report.txt` | SEM 完整报告（拟合指标+路径+载荷） |

### 质性报告（`outputs/tables/`）

| 文件名 | 说明 |
|--------|------|
| `qualitative_analysis_report.txt` | 质性分析完整报告 |
| `sap_exemplar_cases.txt` | 典型案例列表 |

## 🔧 自定义配置

### 修改条目列名

如需使用真实问卷条目名称，请编辑 `src/scales.py`：

```python
GENAI_LITERACY_ITEMS: Dict[str, List[str]] = {
    "ai_knowledge": [
        "Q1",  # 替换为真实列名
        "Q2",
        "Q3",
    ],
    # ... 其他维度
}
```

### 修改统计参数

编辑 `src/config.py`：

```python
ALPHA_LEVEL = 0.05        # 显著性水平
N_FACTORS_EFA = 4         # EFA 因子数
RANDOM_STATE = 42         # 随机种子

ML_PARAMS = {
    "kmeans_n_clusters": 4,   # 聚类数
    "rf_n_estimators": 100,   # 随机森林树数
    "test_size": 0.3,         # 测试集比例
}
```

## 🔬 分析模块详解

### 模块1: 外部基线校准
- 读取外部数据集
- 计算描述统计（均值、标准差、样本量）
- 用于论文中的对比分析

### 模块2: 前后测效能评估
- 计算 GenAI 四维总分
- 配对样本 t 检验
- Cohen's d 效应量
- 输出完整统计表

### 模块3: 量表信效度
- Cronbach's Alpha（总体+各维度）
- KMO + Bartlett 球形检验
- 探索性因子分析（EFA）
- 因子载荷矩阵

### 模块4: 馆社共创机制量化
- 计算共创过程七维总分
- 计算 OSE 参与度四维总分
- 合并数据集供 SEM 使用

### 模块5: SEM 分析
- 构建结构方程模型
- 路径分析：CoCreate → Engage → ΔGenAI → ProductQuality
- 拟合指标：CFI, TLI, RMSEA, SRMR
- 中介效应检验

### 模块6: 学习分析
- 行为日志特征工程
- KMeans 轨迹聚类
- 随机森林产出预测
- AUC 评估
- OULAD 模板展示

### 模块7: 质性三角互证
- 读取 NVivo 编码数据
- 生成 SaP 结果证据矩阵
- 识别典型案例
- 维度统计摘要

## 📝 替换真实数据 Checklist

使用真实海棠杯数据时，请按以下步骤操作：

1. **准备数据文件**
   - [ ] 将所有 CSV/Excel 文件放到 `data/` 对应子目录
   - [ ] 确保文件名与配置一致（或修改 `src/config.py`）

2. **更新列名映射**
   - [ ] 打开 `src/scales.py`
   - [ ] 将默认列名替换为真实问卷条目名称
   - [ ] 保存文件

3. **检查数据格式**
   - [ ] 确保所有数值列为 `float` 类型
   - [ ] 确保有 `user_id` 列（用于数据合并）
   - [ ] 检查缺失值情况

4. **运行分析**
   - [ ] 激活虚拟环境
   - [ ] 运行 `python src/main.py`
   - [ ] 检查控制台输出是否有错误

5. **验证结果**
   - [ ] 查看 `outputs/tables/` 中的表格
   - [ ] 检查数值是否合理
   - [ ] 对比论文预期结果

## ⚠️ 常见问题

### Q1: 某些模块被跳过？
**A**: 检查对应数据文件是否存在，文件名是否正确。程序设计为优雅跳过缺失数据。

### Q2: 列名不匹配错误？
**A**: 编辑 `src/scales.py`，将默认列名替换为你的真实列名。

### Q3: SEM 模型拟合失败？
**A**: 可能是样本量不足或数据质量问题。检查缺失值，尝试使用简化模型。

### Q4: 内存不足？
**A**: 本项目已优化为资源约束笔记本可运行。如仍有问题，减少行为日志数据量。

### Q5: 如何单独运行某个模块？
**A**: 可以在 Python 中导入对应函数：
```python
from src.main import module_pre_post_evaluation
module_pre_post_evaluation()
```

## 📚 依赖说明

核心依赖包：
- `pandas`: 数据处理
- `numpy`: 数值计算
- `scipy`: 统计检验
- `pingouin`: 高级统计（配对 t 检验、信度）
- `statsmodels`: 回归分析
- `factor-analyzer`: 因子分析
- `semopy`: 结构方程模型
- `scikit-learn`: 机器学习
- `lifelines`: 生存分析（可选）

## 🤝 贡献

本项目为学术研究代码，欢迎提出改进建议。

## 📄 许可

本项目仅供学术研究使用。

## 📧 联系

如有问题，请查看 `instruction.md` 详细需求文档或联系项目负责人。

---

**最后更新**: 2025年11月25日
**版本**: 1.0.0
# haitang_sap_study
